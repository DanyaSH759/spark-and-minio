adduser my_user
usermod -aG sudo my_user
groups my_user
sudo whoami

sudo chmod +x bootstrap_spark.sh
ls -l  bootstrap_spark.sh
./bootstrap_spark.sh


sudo usermod -aG docker "$USER"
exec su - "$USER"
groups | grep docker

sudo docker compose down -v
sudo docker compose up -d
sudo docker ps


uv run python -c "import pyspark; print(pyspark.__version__)"

uv run python local_spark_test.py
uv run python spark_to_minio_smoke.py


docker run --rm --network=host --entrypoint sh minio/mc:latest \
  -c "mc alias set local http://000:9000 THE_BEST_LOGIN THE_BEST_PASSWORD && mc ls local"